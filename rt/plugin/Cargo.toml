[package]
name = "opq-plugin"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
crate-type = ["cdylib"]

[dependencies]
opq-engine = { path = "../engine" }
nih_plug = { git = "https://github.com/robbert-vdh/nih-plug.git" }
