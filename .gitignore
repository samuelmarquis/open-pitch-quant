# audio & MIDI are generated (tools/make_probes.py), user-supplied, or
# derived from commercial software — all stay local, out of git
out/
*.wav
*.mid
testdata/reference/
testdata/material/

# python
__pycache__/
*.pyc
.venv/

# rust (future)
target/

# nix
result
result-*

# ableton test project (user's live set)
*Project/
*.als
*.asd

# editor/os
.DS_Store
Icon?

# clap-wrapper SDK checkouts (re-fetch: git clone into wrac/clap_wrapper_builder/
# per wrac/.gitmodules — clap, vst3sdk [--recursive], AudioUnitSDK)
wrac/clap_wrapper_builder/clap/
wrac/clap_wrapper_builder/vst3sdk/
wrac/clap_wrapper_builder/AudioUnitSDK/
wrac/target/
